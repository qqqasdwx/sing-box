name: CI

on:
  pull_request:
    paths:
      - "src/**"
      - "tools/**"
      - "sing-box.sh"
      - "docker_init.sh"
      - "examples/**"
      - "tests/**"
      - ".github/workflows/bundle.yml"
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "tools/**"
      - "sing-box.sh"
      - "docker_init.sh"
      - "examples/**"
      - "tests/**"
      - ".github/workflows/bundle.yml"

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Verify bundled scripts
        run: |
          chmod +x tools/bundle.sh tools/prepare-release.sh
          tools/bundle.sh --check

      - name: Check shell syntax
        run: bash -n sing-box.sh docker_init.sh tools/bundle.sh tools/prepare-release.sh tests/test-v2rayn-hy2.sh

      - name: Test v2rayN Hysteria2 output
        run: bash tests/test-v2rayn-hy2.sh

      - name: Check routing examples
        run: find examples/routing -type f -name '*.json' -print0 | xargs -0 -r -n1 jq empty
